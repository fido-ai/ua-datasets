# Updated requirements to support plugins (autorefs needs MkDocs>=1.5 for event_priority)
mkdocs>=1.5.3,<2.0            # Core; event_priority available from 1.5
mkdocs-material>=9.5.0,<10.0  # Modern theme compatible with MkDocs 1.5+
pymdown-extensions>=10.8,<11.0
mkdocstrings>=0.24.0,<0.25.0  # Current stable API; works with mkdocs>=1.5
mkdocstrings-python>=1.10.0,<2.0  # Separate provider package for newer mkdocstrings
mknotebooks>=0.8.0,<0.9       # Compatible with MkDocs 1.5
mkdocs-autorefs>=1.0.1,<2.0   # Provides autorefs plugin using event_priority
mkdocs-include-exclude-files>=0.0.1
jinja2>=3.1.4,<4.0            # Newer Jinja2 fine with updated mkdocstrings
nbconvert>=7.16.0,<8.0        # Modern nbconvert (Python 3.11 compatible)
nbformat>=5.10.0,<6.0
pygments>=2.18.0,<3.0

# Legacy / project-specific utilities
pytkdocs_tweaks==0.0.6        # Retain existing tweak package (validate compatibility periodically)
